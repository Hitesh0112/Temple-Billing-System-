Download the following:

1) Python 3.10.5(64-bit)

Then open command prompt and open as the administrator download the following using these commands

1) pip install pandas

2) pip install pillow

3) pip install googletrans==3.1.0a0
